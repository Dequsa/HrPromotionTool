import torch
import torch.nn as nn
import pandas as pd
import numpy as np

# load model -> take user input -> parse user input -> run through model -> print the output

class Model(nn.Module):
    def __init__(self):
        super(Model, self).__init__()


if __name__ == "__main__":
    input_handler = InputHandler()
    model_loader.load()

